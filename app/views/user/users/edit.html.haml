= simple_form_for @user, url: user_user_path(@user.id) do |f|
  = f.input :first_name
  = f.input :second_name
  = f.input :email
  = f.input :phone
  = f.input :city_id, collection: @cities, prompt: :translate
  = f.input :password
  = f.submit class: 'btn btn-info'