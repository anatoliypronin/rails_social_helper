%a.pull-right
  %h1= t('users')
%table.table.table-striped.table-hover
  %tr
    %th= User.human_attribute_name(:id)
    %th= User.human_attribute_name(:full_name)
    %th= User.human_attribute_name(:phone)
    %th= User.human_attribute_name(:email)
    %th= User.human_attribute_name(:city)
    %th= User.human_attribute_name(:state)
    %th{:colspan => "2"}= t('.control')
  - @users.each do |user|
    %tr
      %td= link_to user.id, admin_user_path(user.id), target: :_blank
      %td= user.full_name
      %td= user.phone
      %td= user.email
      %td= user.city.name
      %td= user.human_state_name
      %td= link_to t('.edit'), edit_admin_user_path(user.id), class: 'btn btn-info btn-sm'
      %td= link_to t('.delete'), admin_user_path(user.id), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm'
%td= link_to t('.add'), new_admin_admin_path, class: 'btn btn-outline-secondary'